<Page
    x:Class="MyDriving.UWP.Views.ProfileView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MyDriving.UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="PROFILE">

    <Grid Background="{StaticResource MainBackgroundColor2}">
        <ScrollViewer VerticalScrollBarVisibility="Hidden">
            <StackPanel Margin="24,24,24,24">

                <Grid >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <controls:CirclePercentage x:Name="CirclePercentage" Grid.RowSpan="2" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" DataContext="{Binding }"/>
                    <Ellipse Grid.RowSpan="2" Width="100" Height="100">
                        <Ellipse.Fill>
                            <ImageBrush x:Name="ProfileImage" ImageSource="{x:Bind profileViewModel.Settings.UserProfileUrl, Mode=OneWay, Converter={StaticResource ImageSourceConverter}}"/>
                        </Ellipse.Fill>
                    </Ellipse>

                    <TextBlock Name="DrivingSkills" Text="{x:Bind profileViewModel.DrivingSkills, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter=' {0}% '}"   
                                     Grid.Column="1" FontFamily="SegoeUI" FontSize="32" Foreground="{StaticResource BrandColor}" TextWrapping="Wrap"/>
                    <StackPanel Grid.Column="1" Grid.Row="1">
                        <TextBlock Name="DrivingSkillsMessage2" Text="{x:Bind profileViewModel.DrivingSkillsPlacementBucket.Description,  Converter={StaticResource StringFormatConverter}, ConverterParameter='Driving skills: {0}', Mode=OneWay}"   
                                    FontFamily="SegoeUI-Light" FontSize="16" Foreground="White" TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>

                <Border Margin="0,16,0,0" />
                <controls:ProfileViewTabControl x:Name="TotalDistanceTab" DataContext="{Binding TotalDistanceDisplay}" />

                <Border Margin="0,10,0,0" />
                <controls:ProfileViewTabControl x:Name="TotalTimeTab" DataContext="{Binding TotalTimeDisplay}" />

                <Border Margin="0,10,0,0" />
                <controls:ProfileViewTabControl x:Name="MaxSpeedTab" DataContext="{Binding MaxSpeedDisplay}" />

                <Border Margin="0,10,0,0" />
                <controls:ProfileViewTabControl x:Name="FuelConsumptionTab" DataContext="{Binding FuelDisplay}" />

                <Border Margin="0,10,0,0" />
                <controls:ProfileViewTabControl x:Name="HardBreaksTab" DataContext="{Binding HardStops,  Converter={StaticResource StringFormatConverter}, ConverterParameter=' {0}  times'}" />

                <Border Margin="0,10,0,0" />
                <controls:ProfileViewTabControl x:Name="HardAccelTab" DataContext="{Binding HardAccelerations,  Converter={StaticResource StringFormatConverter}, ConverterParameter=' {0}  times'}" />

                <Border Margin="0,10,0,0" />
                <controls:ProfileViewTabControl x:Name="TotalTripsTab" DataContext="{Binding TotalTrips}" />

                <!-- we no longer have tips
                <Border Margin="0,10,0,0" />
                <controls:ProfileViewTabControl x:Name="TipsTab" DataContext="{Binding Tips.Count,  Converter={StaticResource StringFormatConverter}, ConverterParameter=' {0}  tips'}" />
                -->
            </StackPanel>
        </ScrollViewer>

    </Grid>
</Page>
